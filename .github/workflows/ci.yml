name: CI Pipeline

on:
  push:
    branches: [main, feature/*]
  pull_request:
    branches: [main, feature/*]

jobs:
  lint:
    name: Lint (ESLint)
    runs-on: ubuntu-latest
    env:
      AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
      AUTH_COGNITO_CLIENT_ID: ${{ secrets.AUTH_COGNITO_CLIENT_ID }}
      AUTH_COGNITO_CLIENT_SECRET: ${{ secrets.AUTH_COGNITO_CLIENT_SECRET }}
      AUTH_COGNITO_DOMAIN: ${{ secrets.AUTH_COGNITO_DOMAIN }}
      AUTH_COGNITO_ISSUER: ${{ secrets.AUTH_COGNITO_ISSUER }}
      NEXT_PUBLIC_AUTH_COGNITO_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_AUTH_COGNITO_CLIENT_ID }}
      NEXT_PUBLIC_AUTH_COGNITO_DOMAIN: ${{ secrets.NEXT_PUBLIC_AUTH_COGNITO_DOMAIN }}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install pnpm
        run: npm install -g pnpm
      - name: Install dependencies
        run: pnpm install
      - name: Run ESLint
        run: pnpm run lint

  format:
    name: Format (Prettier)
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install pnpm
        run: npm install -g pnpm
      - name: Install dependencies
        run: pnpm install
      - name: Run Prettier
        run: pnpm run format

  audit:
    name: Security Audit (pnpm audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install pnpm
        run: npm install -g pnpm
      - name: Install dependencies
        run: pnpm install
      - name: Run pnpm audit
        run: pnpm audit

  test:
    name: Test (Jest)
    runs-on: ubuntu-latest
    env:
      AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
      AUTH_COGNITO_CLIENT_ID: ${{ secrets.AUTH_COGNITO_CLIENT_ID }}
      AUTH_COGNITO_CLIENT_SECRET: ${{ secrets.AUTH_COGNITO_CLIENT_SECRET }}
      AUTH_COGNITO_DOMAIN: ${{ secrets.AUTH_COGNITO_DOMAIN }}
      AUTH_COGNITO_ISSUER: ${{ secrets.AUTH_COGNITO_ISSUER }}
      NEXT_PUBLIC_AUTH_COGNITO_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_AUTH_COGNITO_CLIENT_ID }}
      NEXT_PUBLIC_AUTH_COGNITO_DOMAIN: ${{ secrets.NEXT_PUBLIC_AUTH_COGNITO_DOMAIN }}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install pnpm
        run: npm install -g pnpm
      - name: Install dependencies
        run: pnpm install
      - name: Install Jest for CI
        run: pnpm add -D jest jest-environment-jsdom @types/jest ts-jest
      - name: Run Jest
        run: pnpm run test

  build:
    name: Build (Vite)
    runs-on: ubuntu-latest
    env:
      AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
      AUTH_COGNITO_CLIENT_ID: ${{ secrets.AUTH_COGNITO_CLIENT_ID }}
      AUTH_COGNITO_CLIENT_SECRET: ${{ secrets.AUTH_COGNITO_CLIENT_SECRET }}
      AUTH_COGNITO_DOMAIN: ${{ secrets.AUTH_COGNITO_DOMAIN }}
      AUTH_COGNITO_ISSUER: ${{ secrets.AUTH_COGNITO_ISSUER }}
      NEXT_PUBLIC_AUTH_COGNITO_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_AUTH_COGNITO_CLIENT_ID }}
      NEXT_PUBLIC_AUTH_COGNITO_DOMAIN: ${{ secrets.NEXT_PUBLIC_AUTH_COGNITO_DOMAIN }}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install pnpm
        run: npm install -g pnpm
      - name: Install dependencies
        run: pnpm install
      - name: Build with Vite
        run: pnpm run build
